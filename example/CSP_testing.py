# from dataAnalyzer import *



# array1 = np.array([np.array([np.array([1,2,3,4,5]) for _ in range(20)]) for _ in range(9)])
# array2 = np.array([np.array([np.array([1,2,4,8,16]) for _ in range(20)]) for _ in range(20)])
# # print(array1.shape)
# print(CSP_filter(array1, array2))